package hr.foi.air.mbankingapp.data.context

import androidx.compose.runtime.compositionLocalOf
import androidx.compose.runtime.mutableStateOf

object Theme {
   var isDarkTheme = mutableStateOf(false)
}